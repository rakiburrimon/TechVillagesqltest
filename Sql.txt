
SELECT * 
FROM results 
ORDER BY mark DESC 
LIMIT 2, 1;



1. Select all from results table
2. Check Descending order to show mark high to low
3. Limit 2, 1 means start count after 2nd index and count upto 1 row (Limit {Index Number}, {Number of Row count} ) 